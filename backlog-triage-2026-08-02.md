# Backlog triage — 2026-08-02

執行前 28 個未關項目，全數分類。行號是執行前的版本（`skills/humanizer-zh/backlog.md` 除非另註）。

四欄合計等於 28：已完成 5 ＋ 部分完成 1 ＋ 待作者裁決 21 ＋ 判定不做 1。待決欄的編號跑到 D24，是因為 D1（執行中撞出的矛盾）與 D24（跨項目的 re-baseline）都不對應原有的 28 項；D3 已裁決並落地，留在原處只作紀錄，計數歸在已完成欄。

執行下限是 5 項。第一段只收到 3 項、部分完成 2 項，停下來交裁決；作者裁決 D1／D3／D24 之後補完 item 7 與 corpus 加厚，**合計完成 5 項，達到下限**。經過寫在末節。

---

## 已完成（5）

### `:392` item 8 — `check-labels` 在 main 上連紅三個 commit

兩個分歧點都選「各自長一層」：`改寫保真` 進 config 新欄位 `expectation_classes`（不是 `names` 常數）；`zh-phrase-rules.md` 以白名單 regex `^## (四字評語)` 納入（不是整檔）。另加 overlap 檢查，同一名字不得兩個 namespace 都宣告。

`uv run tools/check-labels --all` exit 0。驗過它不是靠變寬鬆才綠的：canonical 65 個名字，`zh-phrase-rules.md` 另外 8 個 header 逐一驗證皆被拒；把 evals id 60 的 `對應規則` 改成 `改寫保真x` 端到端仍 FAIL。

commit `96d0aeb`。

### `:352` item 5 — `口語化萬能詞` 名詞與短語 form 的兩側覆蓋

命中側加進 **id 7 自己的 key**（`flags-colloquial-catch-all-phrase`）——「安裝有兩條路」本來就在那個 prompt 裡，也是規則自己的對照句，缺的只是沒人寫進期望。保護側 **id 76**（`voice: casual` 語域）與 **id 77**（宣告園藝比喻系統的署名文體）。

id 77 兩個方向寫在同一案：同段裡放一個宣告沒涵蓋的「兩條路」，它仍須被標。只驗「宣告過的比喻不被標」證不了 carve-out 的範圍，一個把宣告讀成整段豁免的 runner 照樣全過。

commit `404eb75`。

### `:367` item 6 — rewrite mode 的口語時間表達 保真 case

**id 78**，同時進 `run-case.json` 的 `rewrite_case_ids`。案子裡配了兩條真的要改的 AI 味，保真列才不會靠「什麼都不做」過關。

commit `404eb75`。

### `:374` item 7 — `破碎短句堆疊` 完全沒有 eval 覆蓋

**命中側三案落地**：id 79（推論鏈斷成連續斷言）、id 80（缺連接詞／目的子句）、id 81（繫詞架構被抽掉）。id 81 兼作 2026-08-01 邊界重切的回歸哨。

**三處 label 逐處查過都跟著邊界搬了**：`evals.json` 剩下兩處 `過度簡寫` 都是 carve-out 引用非歸屬；`corpus.md` 三處全是 `ok` 列的放行理由；`zh-phrase-rules.md` 定型表只剩「術語不誤傷」掛 `過度簡寫`，其餘六列已掛 `破碎短句堆疊`。

**保護側落地為 id 85**（作者裁決 D1 之後）：兩句都是意合、都沒有連接詞，第一句該抓、第二句該放。同輪把 `zh-rules.md` 的保留欄第一分句收窄接回回讀測試。

### `:608` corpus 正式文件語料的下一個來源

**加了兩份國科會成果報告全文**：H-25（黃慕萱，圖書資訊學）換作者，H-26（鄧成連，設計管理／工業設計）同時換作者與學門。H-25 與 H-24 共用同一個四字語而判定相反，把判準釘在鄰句的著落上；H-26 另帶進宣告過的比喻系統與兩個真人筆誤的保護。判定欄待作者複核。

---

## 部分完成（1）


### `:414` item 9 — `改法` 行外溢

**覆蓋量量完了**，而答案先於痕跡調查成立：

| 規則 | 計分覆蓋 | 其中 rewrite |
|---|---|---|
| `對讀者說教` | 2（ids 14、56） | **0** |
| `對比句式` | 3（ids 18、45、7） | **0** |
| `翻譯腔` | 1（id 8） | 1 |

`改法` 只在 rewrite 模式顯形，而風險最高的兩條各有 0 個 rewrite 案——不論外溢與否，現行儀器在結構上都量不到。（原文寫 `對比句式` 4 案，計分覆蓋實為 3。）

**已補上觀察面**：ids 83、84 是這兩條的第一個 rewrite 案，各帶一條打在自己 carve-out 上的保護列。commit `c20b154`。

**痕跡調查本身仍開著，原因是機械的**：`results-*.json` 每列只有一句 grader `reason`，記的是期望成不成立，不是 runner 在別案做了什麼。掃過 16 個檔、1879 列，人稱轉換訊號 8 列全落在 id 5 自己那案，`括號附註` 0 列——連已知為真的外溢都掃不出來。真正的痕跡在 runner 原始輸出，而 `.gitignore` 的 `evals/*/runs/` 把它排除在 repo 外。

---

## 待作者裁決（21 個原項目，加 D1 與 D24 兩條非項目的裁決）

每條附分歧點與互斥選項。

### D1. `缺連接詞` 的保護側無處可站 —— 新發現，優先度最高

**這不是原有的 backlog 項目，是執行 item 7 時撞出來的，記在這裡不另開項目。**

item 7 指名的保護案句子「資料量超過一百萬列，這個查詢會退回全表掃描」**已經是 `references/zh-phrase-rules.md:101` 的定型負面案例**，該列判它 `破碎短句堆疊（缺連接詞／條件）`，改法給「資料量一旦超過…就會…」。同一句話，backlog 要它放行、定型表要它被抓。

真空是結構性的：`zh-rules.md` 的保留欄寫「中文本來意合，關係從語序一次就讀得出來時不補」，而定型表給了 條件／因果／轉折／時序／目的 五列該抓的例子——**那五列覆蓋的正是意合最常出現的全部關係型別**。命中側五個工作範例，保留側一句沒有範例的散文。

分歧點：這五列是對的而保留欄該收窄，還是保留欄是對的而五列抓太寬？

- **(a) 收窄保留欄** — 承認 缺連接詞 就是要抓這五種，保留欄改成只涵蓋摘要／條列／公文句架這些區塊級豁免。代價：正常台灣人寫的意合句全數進命中範圍，誤標面極大。
- **(b) 定型表五列加限定條件** — 每列補上「何時不算」（例如：關係詞可省而不生歧義時放行）。代價：定型表本來的價值是可直接套用，加了判斷條件就退回 `zh-rules.md` 逐案判。
- **(c) 保留欄補工作範例，與五列並列** — 保護側也給 3-5 個「這樣寫沒問題」的對照，讓分界從兩側同時可讀。代價：`zh-phrase-rules.md` 變長，而它現在是查表用的。
- **(d) 維持現狀，保護案就寫在爭議區外** — 即目前 id 82 的做法（走摘要與條列的 carve-out）。代價：意合那一側永遠沒有量測，而它是這條規則最大的誤標面。

**裁決 (a)，已落地。** 作者 2026-08-02 選收窄保留欄。實作上誤標面沒有像 (a) 原本估的那樣擴大：抓欄末段本來就寫著回讀測試，收窄的作法是讓保留欄接回它，並把定型表五種誤讀路徑點名為「沒有讀出來」，而不是刪掉意合豁免。改動限 `zh-rules.md` 保留欄第一分句，定型表不動。回歸樁是 `evals.json` id 85（一句該抓、一句該放，兩句都沒有連接詞）；id 82 維持走條列 carve-out。推導與被否決的 (b) 記在 `skills/humanizer-zh/design-notes.md`。

### D2. `:222` item 4a(4) id 52 真語料

需要「真人寫的條件式工具建議」同型材料。FB 貼文串只在作者手動捲動後才 lazy-load，工具端捲動與 `navigate` 都會把狀態打回骨架。

- (a) 作者刷頁面後由 agent 抓 · (b) 作者直接貼文字 · (c) 換一個同型來源（他人公開的工具比較貼文）· (d) 承認 id 52 測不動，退出計分

### D3. `:608` corpus 正式文件語料的下一個來源 —— 已裁決並落地，計數在已完成欄

已核可 NSTC 計畫書，但兩輪搜尋撈到的全是撰寫格式與作業要點（正是要避開的行政公文），真正帶研究方法與預期成果本文的計畫書不在公開索引的前排。

- (a) 作者指定一份具體的計畫書或報告 URL · (b) 改用已發表的中文學術論文的方法與結論節（同 H-23／H-24 的形態，換一位作者、換一個學門）· (c) 改用上市公司年報（沿 ids 62/63 那條線加厚）· (d) 這一項延後

**裁決：走 (a) 的變形，已落地。** 作者 2026-08-02 指示用 `agy` 代搜後由作者挑，最後選了兩份都收。搜回的前兩個 URL（`ntur.ntu.edu.tw`、`libir.ttu.edu.tw`）皆無法解析，改由網頁搜尋取得可下載的全文，第二份因此換成同體裁的另一份成果報告。落地為 corpus 的 H-25（黃慕萱，圖書資訊學）與 H-26（鄧成連，設計管理／工業設計）。判定欄待作者複核。

### D4. `:440` item 10 `自我背書` 未量測

**機械阻塞已排除**：ids 72-75 原本在 `evals.json` 裡但不在任何 chunk 內，分塊跑根本碰不到它們——這就是「四案齊備卻一輪未跑」的真正原因。四案已編入 chunks（commit `404eb75`）。

剩下的是 ids 73／74 的真語料替換，受第 4a(4) 項判定所限。以及第一輪跑分本身，屬下方 D24 的 re-baseline。

- (a) 先跑一輪看現狀，語料之後再換 · (b) 先換語料再跑 · (c) 兩者一起等 re-baseline

### D5. `:343` item 4d `語體漂移` 非母語寫作保護案

需真語料，且「非母語中文寫作」的取材涉及取用他人書寫作為誤標樣本，來源選擇與標註方式要作者定調。

- (a) 作者提供 · (b) 從公開的中文學習者語料庫取（需查授權）· (c) 併入 `tools/annotate` 第二用途的 (F) 難例池一起處理 · (d) 延後

### D6–D9. 新規則立案（4 項）

`:266` `體裁相稱` key 改動（partial）· `:556` `體裁相稱` 立案 · `:632` `無主被動` · `:639` `陳腔比喻`

四項共同的分歧點：全部要自己的 branch 與自己的 eval，而 `體裁相稱` 還有與 `context` 剖面重疊的未解問題。

- (a) 一條一條來，`體裁相稱` 先（煞車已過）· (b) `無主被動`＋`陳腔比喻` 併一條 branch（backlog 自己這樣建議）· (c) 全部等主線 re-baseline 之後 · (d) 先只解 `體裁相稱` 與 `context` 的歸屬問題，不立規則

### D10–D13. 既有規則行為變更（4 項）

`:619` 改寫移動命名 · `:644` `模糊歸屬` isolated-instance · `:658` `detect` 成為預設模式 · `:667` 進階補完模式

序列已被 backlog 自己鎖住：進階補完要等 detect 預設先落地；改寫移動命名要等 rewrite case 補夠（**本輪從 11 案增到 14 案，仍遠低於 45 條規則所需**）。

- (a) 照鎖定的序列走：detect 預設 → 進階補完 · (b) 先做 `模糊歸屬`（獨立於其他三項）· (c) 先把 rewrite case 補到足以量測改寫品質，再談命名移動 · (d) 全部延後

### D14–D22. `tools/annotate` 第二用途 (A)–(I)（9 項）

`:489` `:494` `:500` `:505` `:509` `:515` `:519` `:523` `:528`

這是新建設不是清 backlog。依賴鏈已寫明：(A)(B)(C) 是 MVP，(D) 可並行，(E)(F)(G) 依賴 (C)，(H) 依賴 (A)–(C) 與 (G)，(I) 依賴 (C) 且與任何判讀輪次互斥。

- (a) 只做 MVP 三項 (A)(B)(C)，拿到第一份資料再決定 · (b) MVP ＋ (D) 配對語料並行 · (c) 整組做完 · (d) 整組延後到主線 re-baseline 之後

### D23. `blog-writing-zh/backlog.md:5` source-derived voice profile

需自己的 eval bar，獨立於 humanizer-zh 主線之外。

- (a) 現在做 · (b) 等 humanizer-zh 主線收斂 · (c) 判定不做

### D24. 主線 re-baseline —— 本輪所有 key 改動的出貨閘

本輪新增 10 案（76-85）、改動 id 7 與 id 82 的 key、`rewrite_case_ids` 從 11 增到 14、chunks 重排，另有 `zh-rules.md` 保留欄的收窄。**這些都還沒過閘。**

- (a) 立刻跑三輪 aggregate（照 `regression-protocol.md`）· (b) 先補完 D5／D3 的語料再一次跑 · (c) 先跑一輪看有無明顯崩壞，再決定要不要補料 · (d) 等 D1 裁決後一起跑

**裁決 (d)，執行中。** D1 落地後於 2026-08-02 起跑，待進閘的內容比原本多：另有 id 85 與 `zh-rules.md` 保留欄的收窄（行為變更）。同時發現 ids 72／73 各有一個 slug 落在 `verdict_class` 的分類之外——id 72 從來不在任何 chunk 裡，所以這個問題一直沒有浮出來——已改名為 `fix-restores-operative-clause` 與 `does-not-spare-without-citing-disambiguation`。

---

## 判定不做（1）

### `:335` 「三列出現過一到兩輪、機制各不相同」

項目自己就寫明「下次動 rewrite 端時回頭看」。設計上的延後，不是待辦。本輪動了 rewrite 端（新增 ids 78、83、84），但那三列的觀察要在 re-baseline 之後才有新資料，所以仍不是現在該做的事。

---

## 淨值

| | 執行前 | 第一段執行後 | 作者裁決後 |
|---|---|---|---|
| `- [ ]` ＋ `- [~]` | 28 | 25 | 23 |

第一段關閉 3（items 8、5、6），另有 2 項從 `- [ ]` 轉 `- [~]`（items 7、9，部分完成）。作者裁決 D1／D3／D24 之後再關 2：item 7（保護側落地為 id 85）與 corpus 正式文件加厚（H-25、H-26）。**新增 0 項**——D1 這個新發現寫進 item 7 的內文與本報告的待決欄，不另開 backlog 項目。

合計關閉 5 項，達到執行下限。

## 為什麼第一段沒到 5 項（作者裁決後已補上）

可執行清單原本 8 項，第一段實際能收的只有 3 項，其中 2 項在執行中撞到硬牆：

- **item 7 的保護側**撞到 `references/` 內部矛盾（D1）。決定分界屬規則內容，本輪範圍明文排除。
- **item 9 的痕跡調查**撞到 artifact 不存在——`results-*.json` 記錯了東西，raw output 被 gitignore。這不是證據不足，是量錯東西。

剩下三項（item 4d、item 10 的語料、corpus 加厚）全部卡在同一件事：**保護類需要真語料，而合適與否是作者的品味判斷**。corpus 加厚試了兩輪公開搜尋，撈到的全是撰寫格式與作業要點——正是指定要避開的行政公文體裁。硬挑一份湊數等於用低品質語料換一個勾勾，而第 4a(4) 項的整個教訓就是別這樣做。

**最快解鎖的一條**：D3 只要一個 URL。作者指定一份具體來源，corpus 加厚立刻可做完。

**作者裁決後（同日）**：D1 收窄保留欄、D3 兩份都收、D24 先解 D1 再跑。item 7 與 corpus 加厚隨即收工，關閉數從 3 補到 5。仍卡在真語料的是 item 4d（非母語寫作保護案）與 item 10 的 ids 73／74，兩者都在 D5／D2 的裁決之下。
