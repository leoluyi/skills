# Skills backlog

Ideas not yet drafted. Signal: friction hit 2+ times.

## Ideas
- [ ] **infographic-design: resolve the v3 rerun's id-9 key-derivation regression.** 2026-07-19 fresh-session rerun (Haiku, 6 agents) completed mechanically clean — see `skills/infographic-design/evals/ab-protocol-v0.10.1.md` § "v3 rerun". Verdict: no improvement established (nothing cleared 0/2→2/2), but a regression flagged on id-9 expectation [3] (key derivation drawn as converging paths) — control 2/2 pass, treatment 0/2 pass, independently confirmed by grepping the raw SVGs (control has real `<path>` convergence geometry, treatment has only a text caption). The SKILL.md diff shows no content change to the step-9 density-check wording that would plausibly cause this, so causation is unclear at n=2 on Haiku. Next step: a confirmatory run (more reps, id-9 expectation [3] only, ideally Sonnet+) before editing SKILL.md against a signal this thin — do not rationalize it away, but don't chase it with a skill edit either until it's better powered.
- [ ] **Migrate the remaining skills to the skill-creator eval layout.** `infographic-design` now uses `skills/<name>/evals/evals.json` with objectively-verifiable expectations; the other eight still use the legacy top-level `evals/<name>/prompts.json` + `output-quality.json`. `tools/run-eval` reads either, so migration can be incremental. Per skill: merge the two files into the official schema, prune assertions that need human judgment (move those to a `judged-cases.md`), and rename `expected_trigger` to `should_trigger` so the description improver can consume them.

- [x] **infographic-design: add completion criteria to the nine steps.** Done 2026-07-19 (v0.10.0). Each step now ends with a `**Done when**` clause that is checkable and, where it matters, exhaustive — step 1 (position declared + one message), step 4 (every text element at one level), step 5 (every box on grid, every recurring node type has a landmark), step 6 (every quantity, not just the hero, cleared), step 8 (grayscale test). Step 9's gate is no longer carrying the whole load.
- [ ] **infographic-design: convert negation to positive statements.** SKILL.md carries 35 negation tokens (`never` 6, `not` 17, `no` 6, `don't` 3). Per `writing-great-skills`, naming a banned behaviour makes it more available; state the target behaviour instead and keep a prohibition only where the line genuinely cannot be phrased positively, paired with what to do instead. The data-honesty rules in step 6 are the likeliest legitimate guardrails; most of the rest should invert.
- [x] **infographic-design: strengthen or replace the `base position` leading word.** Done 2026-07-19 (v0.10.1). Replaced with the single pretrained word `home`. Rationale: (1) `base` was already taken by the grid's `base unit`, so one word carried two meanings; (2) a single word repeated at every decision site accumulates a distributed definition better than a two-word phrase. It fires at both ends — step 1 **declares** the position (`home`, or a named pull), step 9 **verifies** the build matches, stitched by the `declare`/`declared` thread. A/B read rules (regression sweep + id-7/id-9 density) pre-registered in `evals/ab-protocol-v0.10.1.md`; results appended there.
- [ ] **infographic-design: test whether the main-branch trigger synonyms earn their tokens.** The learn-loop branch was collapsed from seven triggers to one; the main branch still lists several zh/en surface forms (資訊圖表, 懶人包, 圖解, one-pager, visual summary). These are different languages rather than synonyms for one branch, so the cut is a recall question, not a judgment call — run `improve_description.py` against `skills/infographic-design/evals/trigger-queries.json` and let the results decide.

- [x] **avoid-ai-writing-zh: run the GAN adversarial protocol (round 1).** Done 2026-07-17 (n=5 human / 3 AI). FP=0/3 voice-bearing, recall=3/3. Log in `evals/avoid-ai-writing-zh/benchmark-protocol.md`.
- [x] **avoid-ai-writing-zh: decide structure-signals gating for non-blog genres.** Resolved by round 1: expanded auto-enable to voice-bearing genres (casual/blunt, technical-blog/blog personal register, newsletter), kept voice-neutral (docs, RFP, 簽呈, 公文, SOP, investor-email) excluded. Data-backed, not by fiat.
- [x] **Strip development-process noise from skill content.** Done 2026-07-17 (PRs #7 a75b0bd, #8 8ad206b). Removed iteration provenance, derivation narrative, and method-named headers from `avoid-ai-writing-zh/SKILL.md` and 8 `blog-writing-zh/references/*.md`; codified the rule + finishing-check grep in `CLAUDE.md`. Provenance now lives in DEVELOPMENT.md / benchmark-protocol.md / commits.
- [ ] **avoid-ai-writing-zh: GAN protocol round 2 — expand corpus.** Round 1 n is small (3 voice-bearing human / 3 AI). Add more genres and authors, add an English-language bucket, and stress the metaphor-clustering carve-out with more dense-tutorial samples before treating the gate as settled. **Decided 2026-07-17:** target scope is *thorough* — ~20 human / ~12 AI across both languages, run as a Workflow fan-out (generate AI bucket + LLM-judge in parallel). **Blocker (why still deferred):** the human bucket must be verifiably real (protocol: 真人身分以外部考據為準; short excerpts + cited URLs, pre-2022 preferred) — it cannot be fabricated. Needs a real corpus sourced first (web-fetch a proposed list for approval, or user-supplied authors/URLs) before the fan-out can run. Round 1's gate (voice-bearing vs voice-neutral) holds as the settled state until then.
- [x] **avoid-ai-writing-zh: extract English rules to references/ (progressive disclosure).** Done 2026-07-17. SKILL.md 935→565 lines; moved Formatting, Sentence structure, Tier 1/2/3 tables, and the micro-categories (Template phrases … Excessive structure) into `references/english-rules.md` (377 lines) with a pointer in SKILL.md. Kept inline: intro/modes/language-routing, the language-agnostic structural rules (Rhythm/Vocabulary/Paragraph-reshuffle/Treadmill/When-to-rewrite), the full Traditional Chinese section, Severity/profiles/output/tone. Verified: no anchor links pointed into the moved block; the L48 `#結構級訊號` anchor still resolves; the zh "與英文版 X 同源" refs read fine (reference-file header names where Emotional flatline / Reasoning chain artifacts / Acknowledgment loops now live). Version 1.1.1→1.1.2.
- [ ] ...
