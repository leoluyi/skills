# Skills backlog

Ideas not yet drafted. Signal: friction hit 2+ times.

## Ideas

### Issues
- [ ] **`tools/run-eval`: `jq -r '.expected_trigger // .should_trigger'` treats `false` as absent.** Found 2026-07-28 during security review of the nested-session-bug fix (see below); not caused by that fix, and not currently live — every `trigger-queries.json` in this repo today uses `should_trigger`, none use `expected_trigger`. jq's `//` falls through to the right-hand side whenever the left side is `false` (not just `null`), so a fixture using the newer `expected_trigger: false` key (the field name this script's own usage comment says it supports) would silently read as `null` → every negative case would FAIL forever, with no error surfaced. Fix: `if has("expected_trigger") then .expected_trigger else .should_trigger end` instead of `//`.

- [x] **`tools/run-eval` trigger-layer check hit a pre-existing environmental issue.** Done
  2026-07-28 (as part of the avoid-ai-writing-zh refactor's language-prep round). Noted
  2026-07-28 while shipping plain-speak v1.4.0. Every case errored with `no parseable
  decision from claude` — confirmed not specific to that change by rerunning against an
  untouched skill (`avoid-china-writing`), which failed identically. **Root cause (confirmed,
  not the nested-session theory originally suspected):** if `ANTHROPIC_API_KEY` is set in the
  shell environment, the `claude` CLI prefers that metered billing-API path over the
  claude.ai CLI login — in a dev shell where that key is present but uncredited (this repo's
  case), every nested `claude -p` call silently fails with "Credit balance is too low"
  swallowed by the script's `2>/dev/null`. This is the same failure mode already logged
  against the `knowledge-doc-writing`/`plain-speak` resweep below, not a separate bug.
  **Fix:** `invoke_router`'s `claude` branch now runs `env -u ANTHROPIC_API_KEY claude -p ...`
  so the subprocess always uses the free CLI-login router path regardless of what's in the
  parent shell — this tool never wants a billed API call anyway (see the cross-AI-company
  note already in the script). Also surfaces a legible diagnostic (raw output snippet +
  "are you logged into claude.ai via the CLI?") instead of silently swallowing stderr.
  Verified: reran `avoid-ai-writing-zh` (10/10 trigger-queries pass, including ids 3 and 10 —
  see the resweep entry below) and `avoid-china-writing` (11/11 pass) from inside this same
  active Claude Code session, `ANTHROPIC_API_KEY` still set in the parent shell throughout.

- [ ] **avoid-china-writing: two candidate test cases from speak-human-tw, not yet ported.** Noted 2026-07-28, surfaced while porting speak-human-tw's `evals/benchmark.md` into `avoid-ai-writing-zh`'s `evals.json`. SF-14 (中國用語校正 — 視頻/質量/信息/博主/接地氣 in one social post) and SF-15 (半形標點混用 — 中文句子誤用半形逗號/句號/驚嘆號) were excluded from that port because they test the陸用語／簡體殘留 axis, which is this skill's job, not avoid-ai-writing-zh's. Neither has been adapted into this skill's own `evals.json` yet — do that (with attribution, per the pattern in `avoid-ai-writing-zh/NOTICE`) before treating this as done.

- [x] **avoid-china-writing: `term-table.md` is missing 消息 and has no classifier (量詞) rules.** Done 2026-07-28 (v1.1.1). Added 消息（訊息／消息 依語意）與 短信→簡訊 to the table's A 節; put 量詞 inline in SKILL.md's D 語法 pattern list rather than in the table, because SKILL.md states grammar patterns live there in full — a 量詞 subsection in `term-table.md` would have been unreachable without amending that sentence and reopening the routing question the dedup A/B had just settled. Scope is narrow on purpose: 訊息類（一條消息→一則訊息、一條短信→一則簡訊）和 物件類（一部手機→一支手機、一條影片→一支影片）, not a full cross-strait classifier survey. Case 9 rerun single-arm: 6/6 (was 4/6 v1.0.0, 5/6 v1.1.0). No FP regression sweep on the other 11 cases. Original entry: Surfaced 2026-07-28 by eval case 9, which both the v1.0.0 and v1.1.0 arms failed on the same expectation. Given 「还发了一条消息」, both agents converted the characters correctly and then left the vocabulary alone — v1.0.0's agent stated its reasoning outright: 「消息：兩岸用字用詞皆同，無需更動」. It is not the same: 「發了一則訊息」 is the natural Taiwan phrasing. The table lists 信息（資訊／訊息）but not 消息, so the agent had nothing to look up and fell back on a wrong prior. Two fixes, and the second is the larger one: (a) add a 消息→訊息 row; (b) the table has **no 量詞 axis at all** — 一条訊息／一條訊息 vs 一則, 一個項目 vs 一項 are a systematic cross-strait difference that character conversion never touches and vocabulary rows don't cover. Decide whether that's a handful of rows or its own section before writing it. Ship-blocking? No — this gap predates the dedup refactor and affects both versions equally; it was kept out of the v1.1.0 branch on purpose so the committed eval results keep describing the skill that was actually tested.

- [ ] **`tools/run-eval` block-scalar bug had a wider blast radius — resweep the other affected skills.** 2026-07-19. The extractor bug fixed above (see infographic-design item) affected every skill using `description: >-` in frontmatter, not just infographic-design: `avoid-ai-writing-zh`, `blog-writing-zh`, `briefing-outline`, `knowledge-doc-writing`, `plain-speak` (and `learn-loop`, which has no evals yet). Any pass rate recorded for these skills before 2026-07-19 — including the "Fix `tools/run-eval`'s Anthropic-API dependency" item's claim of a verified infographic-design run above — was measured against the truncated `>-` string, not the real description, and should be treated as unverified. Partial resweep with the fixed extractor: `blog-writing-zh` 12/12 clean, `briefing-outline` 10/10 clean. **`avoid-ai-writing-zh` id 3 and id 10 — resolved 2026-07-28.** These were logged as "2 genuine fails, not yet triaged"; now confirmed fixed, and fixed before this round (not by it) — the `ANTHROPIC_API_KEY` credit-exhaustion bug this same entry already suspected below was the actual cause of that unclear read, not a real router miss. Once `tools/run-eval`'s nested-session Issues entry was fixed (see above) and rerun from a clean session, both pass: `avoid-ai-writing-zh` now scores 10/10 on `trigger-queries.json`, including id 3 (en/rewrite) and id 10 (structure-signals). Git-blame on `SKILL.md`'s description shows the structure-signals wording id 10 needs (`e45db5b4`, 2026-07-19 07:59) landed the same day as this resweep — same-day ordering vs. the resweep can't be confirmed from commit messages, but the practical upshot is the description already covered both cases and no rule change was needed this round. Full diagnostic + a residual "blog" wording risk noted in `skills/avoid-ai-writing-zh/design-notes.md`'s "Trigger-query 診斷更正" section. `knowledge-doc-writing` and `plain-speak` resweeps are still inconclusive and still need a clean rerun — same root cause, now fixed in the tool, but those two skills haven't been rerun yet.

- [x] **infographic-design: trigger corpus needs per-synonym isolation before the pruning question is answerable.** Done 2026-07-19. Turned out the ids 4/6/11 misses weren't a synonym-pruning question at all: `tools/run-eval`'s awk extractor only handled single-line YAML scalars, so for every skill whose frontmatter uses a folded block scalar (`description: >-`), it silently fed the router the literal string `>-` instead of the description — the router had been guessing from the skill name alone. Fixed the extractor (now folds multi-line `>-`/`|-` block scalars per YAML semantics). Reran infographic-design 3x with the real description: ids 4 and 11 passed 3/3 immediately (they only ever needed the description text). A genuine, consistent miss then emerged — id 1 (`...資訊圖表...`, the literal trigger word) failed 3/3 — because the data-density exclusion clause ("annual-results decks, survey findings, statistical graphics") was swallowing an explicit infographic ask. Added a carve-out sentence so an explicit infographic/資訊圖表/懶人包/one-pager ask always wins over the data-precision heuristic (v0.11.0→0.11.1). Reran 3x after: 13/13, 13/13, 12/13 (one residual scatter case, id 5, 1/3 — excluded from conclusions per this repo's own scatter rule). No synonym pruning needed; the original premise was an artifact of the eval-tooling bug, not the description.

- [x] **Fix `tools/run-eval`'s Anthropic-API dependency.** Done 2026-07-19. The script curled `api.anthropic.com` directly with a raw API key — a shared-tool cost/portability problem surfaced while working the item above. Rewrote it to run the router judgment as a one-shot subagent call through whatever coding-agent CLI is installed (`claude -p --tools=""` or `codex exec -s read-only`, auto-detected, override via `RUN_EVAL_AGENT`), so it's no longer a direct vendor API call and works across AI coding assistants, not just Anthropic's. Fixed a `set -e`/`pipefail` bug found during verification (a non-matching `grep` on a malformed router reply silently killed the whole loop after one iteration). Verified against `rfp-writing` (9/9, matches the old direct-API run) and `infographic-design`.

- [ ] **plain-speak: two v1.5.0 behaviours are unverifiable from a single-prompt harness — need real
  transcripts in `judged-cases.md`.** 2026-07-30. v1.5.0's bare-invocation path resolves its target
  from the conversation, and cases 8–12 test that against a transcript *embedded in the prompt*. Two
  parts of the resolution order therefore went untested: stepping back over pure tool-work and status
  turns to reach the last turn that carried reasoning, and re-posing a pending question through the
  host's interactive question tool where one exists. Collect real session transcripts where either
  path misfires and turn them into judged cases; do not assume the synthetic runs cover them.
- [x] **plain-speak: add 計畫可執行度 (原則 vs 做法) to the 具體 guardrail.** Done 2026-07-28
  (v1.3.0→v1.4.0). Plan-bearing output was retreating into 原則 ("分階段導入、控制風險")
  instead of 做法 (誰/做什麼/何時) — the existing 具體度 guard only had a descriptive face
  and missed this prescriptive one. A/B needed two iterations: the first eval case (3 moves)
  sat at/below the pre-existing >3-item fold threshold and came back a near-tie (inconclusive
  — see design-notes.md); redesigned to 4 moves and got a clean, reproducible result (v1.4.0
  2/2 kept all numbers, v1.3.0 baseline 0/2, stripped 3 of 4 numbers into vague language both
  times). Log: `skills/plain-speak/evals/results-2026-07-28-plan-concreteness.md`.
- [x] **Migrate the remaining skills to the skill-creator eval layout.** Done 2026-07-19. All 8 skills (avoid-ai-writing-zh, avoid-china-writing, blog-writing-zh, briefing-outline, formal-doc-structure, knowledge-doc-writing, plain-speak, rfp-writing) now use `skills/<name>/evals/{trigger-queries.json, evals.json}`; legacy top-level `evals/<name>/` tree removed, `benchmark-protocol.md` files moved alongside. `expected_trigger`→`should_trigger` renamed throughout. Caveat: `knowledge-doc-writing` had no real trigger file before (its old `prompts.json` was content-quality material mislabeled) — its 6 negative trigger queries are freshly authored from SKILL.md's documented exclusions, not carried over, so treat that one file as unverified until it's actually been run once.

- [x] **infographic-design: add completion criteria to the nine steps.** Done 2026-07-19 (v0.10.0). Each step now ends with a `**Done when**` clause that is checkable and, where it matters, exhaustive — step 1 (position declared + one message), step 4 (every text element at one level), step 5 (every box on grid, every recurring node type has a landmark), step 6 (every quantity, not just the hero, cleared), step 8 (grayscale test). Step 9's gate is no longer carrying the whole load.
- [ ] **infographic-design: convert negation to positive statements.** SKILL.md carries 35 negation tokens (`never` 6, `not` 17, `no` 6, `don't` 3). Per `writing-great-skills`, naming a banned behaviour makes it more available; state the target behaviour instead and keep a prohibition only where the line genuinely cannot be phrased positively, paired with what to do instead. The data-honesty rules in step 6 are the likeliest legitimate guardrails; most of the rest should invert.
- [x] **infographic-design: strengthen or replace the `base position` leading word.** Done 2026-07-19 (v0.10.1). Replaced with the single pretrained word `home`. Rationale: (1) `base` was already taken by the grid's `base unit`, so one word carried two meanings; (2) a single word repeated at every decision site accumulates a distributed definition better than a two-word phrase. It fires at both ends — step 1 **declares** the position (`home`, or a named pull), step 9 **verifies** the build matches, stitched by the `declare`/`declared` thread. A/B read rules (regression sweep + id-7/id-9 density) pre-registered in `evals/ab-protocol-v0.10.1.md`; results appended there.

- [x] **avoid-ai-writing-zh: run the GAN adversarial protocol (round 1).** Done 2026-07-17 (n=5 human / 3 AI). FP=0/3 voice-bearing, recall=3/3. Log in `evals/avoid-ai-writing-zh/benchmark-protocol.md`.
- [x] **avoid-ai-writing-zh: decide structure-signals gating for non-blog genres.** Resolved by round 1: expanded auto-enable to voice-bearing genres (casual/blunt, technical-blog/blog personal register, newsletter), kept voice-neutral (docs, RFP, 簽呈, 公文, SOP, investor-email) excluded. Data-backed, not by fiat.
- [x] **Strip development-process noise from skill content.** Done 2026-07-17 (PRs #7 a75b0bd, #8 8ad206b). Removed iteration provenance, derivation narrative, and method-named headers from `avoid-ai-writing-zh/SKILL.md` and 8 `blog-writing-zh/references/*.md`; codified the rule + finishing-check grep in `CLAUDE.md`. Provenance now lives in design-notes.md / benchmark-protocol.md / commits.
- [x] **avoid-ai-writing-zh: GAN protocol round 2 — expand corpus.** Done 2026-07-28 as the refactor item's step 1. Round 1 n was small (3 voice-bearing human / 3 AI). Target scope (decided 2026-07-17) was ~20 human / ~12 AI across both languages — delivered as `evals/corpus.md`: 20 真人桶 cases (保哥/高見龍/Julia Evans/Simon Willison — 2022 前, strong 考據; 知識倉鼠/90s.pm.investing/AI避坑情報員 — 2023 後具名筆名但無法排除 AI 輔助, marked Tier B; 工程會/IETF RFC — voice-neutral 對照組) and 12 AI 桶 cases (8 topic-aligned to H examples, 4 word-level-clean "voiceless" probes for the structural layer). Every case has a judgment table with exact-substring 引文片段 spans, verified against a parser script (format contract holds on all 32 cases). English bucket (the round-1 gap) now covered by Julia Evans + Simon Willison. Blocker resolved: human bucket sourced via parallel research agents with per-source verification (raw HTML/PDF/RFC text pulled directly, not paraphrased), candidate list approved before fetching.
- [x] **avoid-ai-writing-zh: extract English rules to references/ (progressive disclosure).** Done 2026-07-17. SKILL.md 935→565 lines; moved Formatting, Sentence structure, Tier 1/2/3 tables, and the micro-categories (Template phrases … Excessive structure) into `references/english-rules.md` (377 lines) with a pointer in SKILL.md. Kept inline: intro/modes/language-routing, the language-agnostic structural rules (Rhythm/Vocabulary/Paragraph-reshuffle/Treadmill/When-to-rewrite), the full Traditional Chinese section, Severity/profiles/output/tone. Verified: no anchor links pointed into the moved block; the L48 `#結構級訊號` anchor still resolves; the zh "與英文版 X 同源" refs read fine (reference-file header names where Emotional flatline / Reasoning chain artifacts / Acknowledgment loops now live). Version 1.1.1→1.1.2.
- [ ] **blog-writing-zh: mine `ecc:brand-voice` for a source-derived voice-profile path.** Noted 2026-07-28. (The user said "brain-voice"; no such skill exists — the intended one is `ecc:brand-voice`, an ECC plugin skill at `~/.claude/plugins/marketplaces/everything-claude-code/skills/brand-voice/`.) It builds a *durable* voice profile from 5–20 real samples of the user's own writing (rhythm, compression vs explanation, source priority order, public-launch vs private-working voice split) and reuses that profile across channels instead of re-deriving style per piece. blog-writing-zh currently derives voice the other way: fixed presets over composable axes (opening × persona × metaphor × closing), modeled on seven *studied* blogs, not on the user's own corpus. Worth evaluating whether to add a "用我自己的既有文章當風格來源" path — a profile extracted once and cached in the repo, then fed to the axes as a preset — versus keeping the studied-blog presets as the only recipe source. Constraints: the ECC skill is a plugin, so per this repo's portability rule it can be a pointer at most, never load-bearing; and any new path needs its own eval bar before shipping.

- [ ] **avoid-ai-writing-zh: refactor to simplify while raising effect, backed by current skill-authoring guidance.** Noted 2026-07-28. Three linked strands: (a) refactor, (b) evals, (c) tooling.

  **Execution order (decided 2026-07-28, supersedes the earlier "refactor → evals → tooling" ordering).** Both (b) and (c) split in half: the *decision* half moves early, the *engineering* half moves late. Short form: 格式先定 → 資料先齊 → 才動 SKILL.md → 量 → 最後才自動化.

  0. **[x] Settle the judged-case format first — decision only, build nothing.** Done 2026-07-28. From (c)'s open design question: inline markers in Markdown vs a side-car JSON of line/span + verdict. Decided: **淨引文塊 + 引文片段對照表** — a verbatim blockquote (the clean quote) followed by a judgment table whose 引文片段 column is an exact substring of that quote (not a character offset, so editing the quote elsewhere doesn't break alignment). Full contract written into `evals/corpus.md` itself (解析契約 section) and verified by a disposable parser script against all 32 real cases.
  1. **[x] Corpus + (b)'s test-data half — build `evals/corpus.md` before touching SKILL.md.** Done 2026-07-28. Delivered as two files, not one, after a mid-build correction: speak-human-tw's 42 SF/SNF cases turned out to be shaped like eval cases (prompt/expected-direction), not raw corpus material, so they went into **`evals.json`** (ids 15–54, 40 cases, SF-14/15 excluded — see avoid-china-writing note below) rather than `corpus.md`; **`corpus.md`** ended up holding only the real-text 真人／AI buckets (20 + 12, see the GAN-round-2 entry above). Also done in this pass: fixed the three answer-leaking prompts (ids 1/2/4 — parenthetical ground-truth moved out of the prompt into `expectations`), added 2 voice-neutral false-positive cases (ids 13–14, docs/簽呈 must not auto-enable 結構級訊號). The "two untriaged fails" turned out to be a different file than assumed — id 3/id 10 are `trigger-queries.json` query ids, not `evals.json` case ids (both files happen to have those numbers) — diagnosed via a router-judgment subagent and then confirmed for real once step 2's tool fix landed: **both already pass** (10/10), fixed by an earlier, unrelated description edit (`e45db5b4`, 2026-07-19), not by this round. Full diagnosis in `skills/avoid-ai-writing-zh/design-notes.md`. Rebuilt the frozen baseline against unmodified `main` SKILL.md: 87/88 expectations pass, 0 protection-class failures, 1 hit-class miss (id 18, logged as a step-3 watch item) — see `evals/results-2026-07-28-corpus-baseline.md`. This supersedes the 2026-07-21 9/9 result.
  2. **[x] Optional gate: fix `tools/run-eval`'s nested-session bug** (its own Issues entry above). Done 2026-07-28, last in this round per plan. Turned out not to be a nested-session bug at all — root cause was `ANTHROPIC_API_KEY` credit exhaustion taking auth precedence over the CLI login; see the Issues entry for the fix. Worth having done anyway: confirmed the id-3/id-10 resolution above with the real tool instead of only a manual diagnostic.
  3. **Then branch and refactor** — the id 3 / id 10 question from step 1's original framing is already resolved (see above, no fix needed there); this step's rule-level work is now just the refactor itself (strand a) plus the id-18 and id-50/51 watch items logged in the baseline results. Cut on a dedicated branch with the corpus already frozen, so before/after is measured against fixed test data rather than a moving target. Branch `refactor/avoid-ai-writing-zh` already exists.
  4. **Then A/B the branch against main** on the frozen corpus, and **continue all further optimisation on the refactored version** — post-refactor is the new baseline; don't keep tuning the pre-refactor SKILL.md in parallel or the two lines diverge and neither has a clean comparison.
  5. **Last: build (c)'s mark-and-iterate tool.** Its value is driving the *next* iteration, not this one, and step 1 having been done by hand is what tells you where the real friction is — a tool designed before that encodes guesses. The format was already settled in step 0, so deferring the build costs no rework. **The trap this ordering avoids:** building the tool first bakes the current rule structure into its format, and the refactor is precisely what dismantles that structure. **New finding from step 1 (2026-07-28) that sharpens (c)'s case:** `evals.json` grew from 9 to 54 cases in this round, and there is currently **no tool that runs it at all** — `tools/run-eval` only exercises `trigger-queries.json` (whether the router fires), never `evals.json`'s content-quality cases. The step-1 baseline run (see above) was done entirely by hand: 6 parallel runner agents + 6 parallel grader agents, manually chunked and manually composed. That was tolerable once, for freezing one baseline; it will not be tolerable for step 3's repeated ablation rounds. This is a stronger case for automation than the mark-and-iterate tool alone, but the ordering argument still holds — build it after step 3 shows what the ablation loop actually needs (e.g. does it need per-chunk parallel dispatch baked in, or just a single-shot runner+grader pair), not before.

  **(a) Refactor: cut rules, keep the priors the model doesn't have.** Primary reference — [The new rules of context engineering for Claude 5 generation models](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models). Its thesis reframes this whole item: Anthropic removed **over 80% of Claude Code's own system prompt** for Claude 5 with no performance loss, because the newer models need far less explicit instruction. Rules → judgment ("write code that reads like the surrounding code" replacing "never write multi-paragraph docstrings"); repetition → conciseness; upfront loading → progressive disclosure. Secondary, for mechanics only: Anthropic's [skill authoring best practices](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) and Matt Pocock's `writing-great-skills` (local: `~/.agents/skills/writing-great-skills/` + `GLOSSARY.md`).
    - **The real thesis: most of this skill is a rule list Claude 5 no longer needs.** The body is two flat catalogs of ~50 `###` micro-categories each — English AI-isms (L147–402) and 繁中 AI-isms (L402–760). The English catalog is the prime suspect: "delve", "it's not just X, it's Y", em-dash abuse, hedge stacking are strong priors the model already holds, and spelling out 50 of them is the exact "explicit constraints" pattern the post says to retire. The load-bearing content is what the model *lacks* priors for — the 繁中/台灣 layer (translation-ese, 陸味, slogan lines) and the structure-signals gate (naming what's *absent*: no stance, no concrete detail, no self-made metaphor). Test the strong version: cut the English catalog to a short judgment-shaped statement plus the existing `references/english-phrase-rules.md` pointer, and see whether evals move. If they don't, that's ~250 lines gone for free.
    - **Length is a symptom, not the target.** `SKILL.md` is 965 lines. This is regression, not new growth: 488f51a extracted the English rules (935→565), d7347fe partially undid it (only the phrase lookup tables stayed in `references/` — `english-phrase-rules.md` 166 lines, `zh-phrase-rules.md` 81), then five commits on 07-19/20 stacked more on. Pocock's name for this is **sediment**. Re-extracting is the weak fix; deleting is the strong one — and the branch structure (language routing already splits en / zh-tw / mixed) makes disclosure the right home for whatever survives the cut.
    - **Negation.** 130 negation tokens (`never` / `don't` / `avoid` / `not`). Same defect already logged against infographic-design, and the same direction the post points: state the target behaviour, trust judgment. Note the awkward bit — this skill's *subject matter* is a list of things not to write, so full inversion may not be reachable; aim at the instruction layer, not the pattern catalog.
    - **Leading words.** Look for restatements that collapse into one pretrained word — the move that turned infographic-design's `base position` into `home`. Severity tiers and the structure-signals gate are the likeliest candidates. This is compression, so it serves the cut rather than fighting it.
    - **Completion criteria — apply with restraint.** Adding a checkable **Done when** to each mode helps "iterate to convergence" stop being self-judged, but it cuts against "trust judgment". Add only where a mode actually terminates early in practice; don't scaffold all four on principle.
    Constraint: measured bar, not a shorter-is-better bar — the refactor must beat the current version on `evals/evals.json`. Given the 80%-cut precedent, run it as an ablation (drop a block, re-run, keep the cut if the score holds) rather than a single rewrite.

  **(b) Evals: fix the answer-leaking prompts, then fill the gaps.** *Split across steps 1 and 3 — test-data fixes in step 1, rule-level fixes in step 3.* Only 9 evals, and 3 of them hand the model the answer inside the prompt: id 1 ends `（此段無 AI 空話病句，句子文法正確。）`, id 2 pre-announces `（作者刻意保留：第一人稱立場句、自創比喻、口語破格）`, id 4 states `（有立場、有具體、有口語破格，但無自創比喻…）`. Each one pre-labels exactly the judgment under test — the run measures compliance with the hint, not detection. Rewrite them as bare text with the labels moved into `expectations` only, then re-baseline (every pass rate recorded against these three is void). Gaps to fill after: the two untriaged fails from the 07-19 resweep (id 3 en/rewrite, id 10 structure-signals), plus false-positive cases on voice-neutral genres.

  **(c) New tool: a mark-and-iterate loop for example articles.** *Split across steps 0 and 5 — format decision in step 0, build in step 5.* Want a low-friction way to point at a sentence in a sample article and say "this one is wrong / this one is fine", accumulating a judged corpus that drives the next iteration — instead of hand-editing JSON. Note there is already `evals/judged-cases.md` and `evals/adversarial-eval-protocol.md`; decide whether this is a script over that format (`tools/`-level, like `run-eval`) or a skill. Design question to settle first: what the marked-up input looks like (inline markers in a Markdown file vs a side-car JSON of line/span + verdict) — the format determines whether the loop can be scripted at all. Should generalize beyond this one skill; `blog-writing-zh` and `avoid-china-writing` have the same need. The Claude-5 post also argues **simple specs → rich references**: rubrics and test suites beat prose descriptions as executable references. That makes the judged corpus this tool produces more valuable than the prose rules it would replace — it's the artifact strand (a) cuts *toward*, not just a testing convenience.

  **Sources** (read these before starting (a) — the refactor thesis rests on the first one):
  - [The new rules of context engineering for Claude 5 generation models](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models) — Anthropic, the primary reference. Rules → judgment, repetition → conciseness, upfront → progressive disclosure, simple specs → rich references; the 80%-system-prompt-cut precedent.
  - [Skill authoring best practices](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) — Claude Docs. Mechanics only: the <500-line guideline, one-level-deep references, ToC on reference files >100 lines, evaluation-driven development loop.
  - Matt Pocock, `writing-great-skills` — local at `~/.agents/skills/writing-great-skills/SKILL.md` + `GLOSSARY.md`. Source of the vocabulary used above: **sediment**, **sprawl**, **no-op**, **negation**, **leading word**, information hierarchy, the branch test.

- [x] **Output Language guard's third paragraph — tested whether removing it causes English leakage into generated headings.** Done 2026-07-28. Prompted by `engineering-guidelines.md`'s new "don't spend tokens on what the model already does" rule — the guard's last paragraph ("The English in this file is structural labelling for you, not literal output. Never mirror this file's language into your response.") is copied verbatim into 9 skills, and is exactly the kind of defensive-against-an-older-model prose that rule targets. Ran 8 parallel agents (2 skills × with/without the paragraph × 2 rounds): `formal-doc-structure` (簽呈, 執行計畫 tasks) showed zero leakage either way — its document types are already named in Chinese in the skill body, so there was no English vocabulary to reach for regardless of the guard. `knowledge-doc-writing` (Diátaxis tutorial/how-to/reference/explanation headings — the actual risk case) showed no consistent effect: round 1 was identical both ways; round 2 went the *opposite* direction (with-guard led headings with English, without-guard led with Chinese-plus-parenthetical-English). n=2 per condition is sampling noise, not a signal, and what noise there is argues against the guard doing what its prose claims. Confound: `knowledge-doc-writing`'s own S5 rule ("keep established terms in English") gives the model an independent, on-topic reason to keep the Diátaxis names as-is, so this pair may be a weak test bed for the general leakage-guard question regardless. **Decision: leave the guard text in all 9 skills unchanged** — 4 runs on one skill pair isn't enough to demote a rule per this repo's own regression-test-before-cutting standard — but treat it as *untested-but-plausible*, not validated; don't cite this guard as proven when discussing further trims. A real test would need a skill whose body does not already instruct keeping some term in English, so the only English pull on the model is the SKILL.md's own structural labels.
- [ ] ...
