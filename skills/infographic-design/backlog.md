# infographic-design backlog

Repo-level and `tools/` items live in the root [`backlog.md`](../../backlog.md).

- [ ] **Convert negation to positive statements — in `references/`, not `SKILL.md`.** The
  original entry (2026-07-19) targeted `SKILL.md`'s 35 negation tokens; the wording rounds since
  have already taken it to **6** across 286 lines, so that half is effectively closed. The load
  moved down the hierarchy: `references/` now carries **147** tokens
  (`never`/`not`/`no`/`don't`/`avoid`), concentrated in `svg-construction.md` (44),
  `learn-loop-viz.md` (23), `bytebytego-style.md` (21), `color-typography.md` (20) and
  `charts.md` (17). Per `writing-great-skills`, naming a banned behaviour makes it more
  available; state the target behaviour instead. Expect a lower yield here than in `SKILL.md`:
  `svg-construction.md`'s prohibitions are largely hard technical guardrails (attributes and
  constructs that genuinely break rendering), which is the case where a prohibition earns its
  place — provided it is paired with what to do instead. The style and layout files are the
  likelier wins.
