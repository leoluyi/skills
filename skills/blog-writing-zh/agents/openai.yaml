# Codex skill metadata. `allow_implicit_invocation: false` pairs with
# `disable-model-invocation: true` in SKILL.md — user-invoked in both harnesses
# or neither.
interface:
  display_name: "Traditional Chinese Blog Writer"
  short_description: >-
    將主題、筆記或外文素材寫成有人味的台灣繁體中文文章。
    Write or rewrite a zh-TW blog post or newsletter.
policy:
  allow_implicit_invocation: false
